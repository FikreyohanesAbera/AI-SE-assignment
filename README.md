## Local Testing

1. Install required dependencies

npm install

2. Running the test

npm test


## Running tests with Docker

1. Build the Docker image

docker build -t ai-js-ts-assignment-eskalate .

2. Run it in a container

docker run --rm ai-js-ts-assignment-eskalate
